// NOTE: BigIntFile的Add实现
// lmut t = BigInt(Bank::BankStore.Read());
// t.Add(BigInt(5));
// Bank::BankStore.Write(t.toString());

// NOTE: PasswordFile的增加与减少
// var.addnum(intv);
// var.minusnum(intv);
